<configuration>

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %-5level %logger{30} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Silence noisy framework output -->
  <logger name="org.apache.spark"                          level="WARN"/>
  <logger name="org.apache.hadoop"                         level="WARN"/>
  <logger name="org.apache.kafka"                          level="WARN"/>
  <logger name="org.apache.pekko"                          level="INFO"/>
  <logger name="org.apache.hadoop.util.NativeCodeLoader"   level="ERROR"/>
  <logger name="dev.ludovic.netlib"                        level="ERROR"/>

  <!-- Our application code logs at INFO -->
  <logger name="com.pipeline" level="INFO"/>

  <root level="WARN">
    <appender-ref ref="CONSOLE"/>
  </root>

</configuration>